language: objective-c
osx_image: xcode7.2
script:
  - xcodebuild -scheme JSONUtilities -configuration Debug -sdk iphonesimulator9.2 -destination "OS=9.2,name=iPad 2" test -enableCodeCoverage YES
after_success:
  - bash <(curl -s https://codecov.io/bash)
  - pod lib lint JSONUtilities.podspec 